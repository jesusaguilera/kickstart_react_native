import {header} from './header';
import {footer} from './footer';
import {logo} from './logo';
import {button} from './button';

const c_ = {
  // Header
  header: header,

  // Footer
  footer: footer,

  // Logo
  logo: logo,

  // Buttons
  button: button,
};

export default c_;
