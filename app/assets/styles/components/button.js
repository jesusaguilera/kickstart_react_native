import {settings} from '../settings';

export const button = {
  go_back: {
    borderRadius: settings.$size_m,
    borderColor: settings.$white,
    width: settings.$size_xl,
    height: settings.$size_xl,
  },
};
